// environment.ts (Mode Développement)

export const environment = {
  production: false,
  // 💡 C'est l'URL de base de votre API Backend (Ex: Express ou Node.js)
  // Ajustez le port (ici 3000) et le préfixe (/api) si nécessaire.
  apiUrl: 'http://localhost:3000/api', 
};